<header
  class="py-3 px-6 w-full bg-gray-50 border-b-2 border-t-4"
>
  <div
    class="flex flex-col md:grid md:grid-cols-3 place-items-center items-center w-full text-rose-900"
  >
    <a href="{{ site.url }}" class="menu-logo img-replace">
      <h1 class="font-cursive text-4xl text-center">{{ site.name }}</h1>
    </a>
    {% include "partial/triad.twig" %}
    <h3 class="text-xs text-center">{{ site.description }}</h3>
  </div>
</header>
